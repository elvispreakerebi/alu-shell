#!/usr/bin/env bash
# This script extracts and displays the visitor IP and HTTP status code from the Apache log file

# Define the path to the Apache log file
LOG_FILE="apache-access.log"

# Use command substitution to indirectly call awk to parse the log file and extract the IP address and HTTP status code
$(echo ${0%%/*}) '{print $1, $9}' "$LOG_FILE"
